language: cpp
os: linux
before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq libboost-dev libboost-locale-dev libcurl4-gnutls-dev
script: make && make test
notifications:
  email: false

sudo: false
language: cpp
compiler:
  - gcc
os: linux
script: make
notifications:
  email: false
addons:
  apt:
    packages:
    - libboost-locale-dev
    - libboost-dev
    - libboost-system-dev
    - libboost-filesystem-dev
    - libcurl4-gnutls-dev

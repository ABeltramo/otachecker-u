sudo: false
language: cpp
compiler:
  - gcc
os: linux
script: make
notifications:
  email: false
addons:
  apt:
    packages:
    - libboost-locale-dev
    - libboost-all-dev
    - libcurl4-gnutls-dev
